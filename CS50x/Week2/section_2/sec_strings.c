#include <cs50.h>
#include <stdio.h>
#include <string.h> // include when working with strings

int main(void)
{
    string user_name = "Emma";
     string

    for (int i = 0; i < length; i++)
    {
        printf ("%c", user_name[i]); // character
        printf ("%i", user_name[i]); // ascii number
    }
    printf("\n");

}